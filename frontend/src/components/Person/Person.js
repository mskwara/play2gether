import React from "react";
import "./Person.scss";

const Person = (props) => {
    return (
        <div id="Person" className={props.className}>
            Tu będzie jakiś player
        </div>
    );
};

export default Person;
